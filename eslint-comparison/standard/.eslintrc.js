module.exports = {
  "extends": "standard",
  "installedESLint": true,
  "plugins": [
    "standard"
  ]

  // this is what you get when you use eslint --init and choose Standard
  // however, you also need to manually install eslint-plugin-promise
};
